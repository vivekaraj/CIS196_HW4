<table>
	<tr>
		<td>name</td>
		<!--<td>category</td>
		<td>company</td> -->
		<td>email</td>
	</tr>
	<% @employees.each do |employee| %>
	<tr>
		<td><%= link_to employee.name, employee_path(employee.id) %></td>
		<!--<td><%= employee.category %></td>
		<td><%= employee.company %></td> -->
		<td><%= employee.email %></td>
		<td><%= link_to "edit employee", edit_employee_path(employee.id) %></td>
		<td><%= link_to "delete employee", employee, confirm: "are you sure you want to delete this employee?", method: :delete %> </td>
	</tr>
	<% end %>
</table>
<p> <%=link_to "Go to home page", root_path %> </p>